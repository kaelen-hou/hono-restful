# AGENTS Rules

## Runtime
- Use Node.js `24.12.0` for local development and CI.
- Use npm `11.6.0` with this Node version.

## Commit Gate
- Run tests before commit (`npm test`) and only commit after tests pass.
